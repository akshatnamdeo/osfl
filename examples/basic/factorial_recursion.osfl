frame Main {
    func factorial(n) {
        if (n == 0) {
            return 1;
        } else {
            return n * factorial(n - 1);
        }
    }

    func main() {
        var result = factorial(5);
        print("Factorial of 5 is:", result);
        return 0;
    }
}
